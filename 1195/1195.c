#include <stdio.h>
#include <string.h>

#define ABLE 0
#define UNABLE 1

int main(void)
{
    char gear_1[101];
    char gear_2[101];
    int min_length = 200;

    scanf("%s", gear_1);
    scanf("%s", gear_2);

    int len_a = strlen(gear_1);
    int len_b = strlen(gear_2);

    for(int i = 0; i < len_a; i++){
        for(int j = )
    }


    printf("%d", min_length);
    
    return 0;
}